[package]
name = "minit-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
nix = { version = "0.29", features = ["process", "signal"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
signal-hook = "0.3"
